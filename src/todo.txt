
- Introduce cpu pointers and add appropriate dispatching. Eliminate the use of raw pointers. CPU dispatching should have openmp capability. Once openmp backend works and is efficient, remove the localTG layer. This is likely more efficient in multicore systems, but complicates the code significantly and is not useful in gpu/accelerators.

Recomendaciones de Alfredo:
- Timing/Performance: Caliper (from LLNL)

- If you need to have access again to H(i,j) and H(i,j,k,l), then write a routine in
  Hamiltonian that generates a new object that implements these, e.g. another variant that encapsulates matrix element access.

- why is hdf_wrapper being compiled? Make this a header only library!

